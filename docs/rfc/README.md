# AuroraBot vNext RFC

此目录保存新架构的唯一设计基准。实现变更应先更新对应 RFC，再修改代码。

建议的编号顺序：

1. `0001-architecture.md`：边界、术语、模块职责与事件生命周期。
2. `0002-configuration.md`：TOML 合并规则、环境变量与 JSON 数据边界。
3. `0003-plugin-contract.md`：应用与认知节点的扩展、发现和加载契约。

