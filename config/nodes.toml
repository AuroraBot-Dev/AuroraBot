# 认知节点图与节点参数。
# 内置节点和扩展节点均须显式启用；安装扩展不会自动加入运行时。

# [[node]]
# id = "builtin.decide"
# enabled = true
# implementation = "aurorabot.nodes.decide:plugin"
# inputs = ["message.received", "effect.succeeded", "effect.failed"]
# outputs = ["effect.requested"]

# [[edge]]
# event_type = "message.received"
# target = "builtin.decide"
