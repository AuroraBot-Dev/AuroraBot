# AuroraBot 扩展

此目录是第三方认知节点、平台适配器和原生应用的推荐存放位置；安装位置本身不代表启用。扩展必须在对应
TOML 配置中显式声明并启用，并遵守 [RFC 0004](../docs/rfc/0004-plugin-contract.md)。

扩展不得直接修改 `data/kernel/`，不得把平台私有对象传入 Node，也不得绕过 Platform 执行外部效果。
第三方 Python 插件自动发现、签名和来源验证不属于当前扩展契约；在后续 RFC 接受前，应通过显式配置接入。
