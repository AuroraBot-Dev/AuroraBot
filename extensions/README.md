# vNext 扩展

此目录是第三方扩展的推荐存放位置；安装位置本身不代表启用。应用、平台适配器和节点必须在相应 TOML 配置中显式启用，并遵守 [RFC 0004](../docs/rfc/0004-plugin-contract.md)。

扩展不得直接修改 `data/kernel/`，不得把平台私有对象传入节点，也不得依赖 `legacy/` API。当前契约仍为草案；在 RFC 0004 接受前，请勿把本目录当作稳定发布接口。
