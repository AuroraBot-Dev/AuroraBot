# 日志配置 — 进程日志级别与目录。
#
# 运行时日志文件名固定为 <log_dir>/aurora.log。Console/Panel 的终端可见性
# 分别由 runtime.console.terminal_logs 与 platforms.toml 的 terminal_logs
# 控制，不在这里覆盖。

[logging]
# 日志级别：DEBUG / INFO / WARNING / ERROR；级别越粗，文件越省
level = "INFO"
# 日志目录；相对项目根，运行时文件名固定为 logs/aurora.log
log_dir = "logs"
