# 本文件仅供本地开发注入密钥。
# 它不定义架构、运行图或应用启用状态；这些内容由 config/*.toml 管理。
# Aurora 核心只读取 TOML 显式引用的密钥；stdio MCP App 只继承 apps.toml 的 env 名单。
# 本文件不会被应用自动加载；从源码运行时使用 uv run --env-file .env aurora。

DEEPSEEK_API_KEY=
# XIAOMI_MIMO_API_KEY=
# OPENAI_API_KEY=
# SILICONFLOW_API_KEY=

# 可选 MCP App 密钥：仅在 config/apps.toml 启用对应 App 时使用。
# AURORA_QQ_TOKEN=

# 可选：选择 config/profiles/<name>.toml。未设置时由 config/runtime.toml 决定。
# AURORA_PROFILE=dev
