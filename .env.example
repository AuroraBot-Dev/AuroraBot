# 本文件仅供本地开发注入密钥。
# 它不定义架构、运行图或应用启用状态；这些内容由 config/*.toml 管理。
# 运行器只读取 RFC 0002 中被 TOML 显式引用的环境变量。

DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
# SILICONFLOW_API_KEY=

# 可选：选择 config/profiles/<name>.toml。未设置时由 config/aurora.toml 决定。
# AURORA_PROFILE=dev
